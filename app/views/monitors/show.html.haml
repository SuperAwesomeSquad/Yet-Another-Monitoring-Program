.row
  .large-9.columns
    %h2.subhead #{@monitor.name}


    ="Description: #{@monitor.description}"
    %br
    %i="Last updated: #{@monitor.updated_at}"
    %p

    =link_to "Edit Monitor", edit_monitor_path(@monitor)
    %br
    =link_to "Delete Monitor", monitor_path(@monitor), :method => :delete, :confirm => "Are you sure you want to delete this monitor?"
